# 📋 BUILD INSTRUCTIONS - Створення EXE з автооновленням

## 🎯 Концепція

```
launcher.exe → завантажує → conversion_monitor.py → запускає
     ↓
  GitHub/Server (завжди свіжа версія)
```

## 🔧 Крок 1: Встановлення PyInstaller

```powershell
pip install pyinstaller
```

## 🚀 Крок 2: Компіляція Launcher в EXE

### Базова компіляція (один файл):
```powershell
pyinstaller --onefile --windowed --icon=app.ico --name="ConversionMonitorLauncher" launcher.py
```

### Розширена компіляція (з логотипом):
```powershell
pyinstaller --onefile `
  --windowed `
  --icon=app.ico `
  --name="ConversionMonitorLauncher" `
  --add-data "version.json;." `
  --hidden-import=urllib `
  --hidden-import=tkinter `
  launcher.py
```

### Параметри:
- `--onefile` - один EXE файл
- `--windowed` - без консолі (для GUI)
- `--noconsole` - альтернатива для --windowed
- `--icon=app.ico` - іконка програми
- `--name` - назва EXE файлу

## ☁️ Крок 3: Налаштування сервера для оновлень

### Варіант A: GitHub (безкоштовно)

1. **Створіть публічний репозиторій на GitHub**
2. **Завантажте файли:**
   - `conversion_monitor.py`
   - `version.json`

3. **Отримайте RAW URLs:**
   ```
   https://raw.githubusercontent.com/USERNAME/REPO/main/conversion_monitor.py
   https://raw.githubusercontent.com/USERNAME/REPO/main/version.json
   ```

4. **Оновіть URLs в launcher.py:**
   ```python
   UPDATE_URL = "https://raw.githubusercontent.com/USERNAME/REPO/main/conversion_monitor.py"
   VERSION_URL = "https://raw.githubusercontent.com/USERNAME/REPO/main/version.json"
   ```

### Варіант B: Власний сервер

1. **Створіть папку на сервері:**
   ```
   /var/www/updates/
   ├── conversion_monitor.py
   └── version.json
   ```

2. **Налаштуйте CORS (якщо потрібно)**

3. **Оновіть URLs:**
   ```python
   UPDATE_URL = "https://yourdomain.com/updates/conversion_monitor.py"
   VERSION_URL = "https://yourdomain.com/updates/version.json"
   ```

### Варіант C: Google Drive (публічний доступ)

1. Завантажте файли на Google Drive
2. Зробіть публічними
3. Отримайте direct download links

## 📦 Крок 4: Структура проекту

```
project/
├── launcher.py              # Лаунчер (компілюється в EXE)
├── conversion_monitor.py    # Основна програма (завантажується)
├── version.json            # Інформація про версію
├── app.ico                 # Іконка (опціонально)
└── build_launcher.bat      # Скрипт для збірки
```

## 🎨 Крок 5: Створення іконки (опціонально)

```powershell
# Конвертуйте PNG в ICO онлайн або через ImageMagick
magick convert icon.png -define icon:auto-resize=256,128,64,48,32,16 app.ico
```

## 🔄 Крок 6: Процес оновлення

1. **Користувач запускає:** `ConversionMonitorLauncher.exe`
2. **Launcher перевіряє версію** на сервері
3. **Якщо є оновлення** - завантажує нову версію
4. **Запускає** `conversion_monitor.py` з кешу

### Де зберігаються файли:
```
Windows: C:\Users\USERNAME\.conversion_monitor\
macOS:   /Users/USERNAME/.conversion_monitor/
```

## 📝 Крок 7: Оновлення версії

1. **Внесіть зміни** в `conversion_monitor.py`
2. **Оновіть** `version.json`:
   ```json
   {
       "version": "1.0.1",
       "release_date": "2025-11-18",
       "changelog": ["Додано нову функцію"]
   }
   ```
3. **Завантажте** обидва файли на сервер/GitHub

Користувачі автоматично отримають оновлення при наступному запуску!

## 🛠️ Крок 8: Скрипт для швидкої збірки

Створіть `build_launcher.bat`:

```batch
@echo off
echo 🔨 Building Conversion Monitor Launcher...
pyinstaller --onefile --windowed --icon=app.ico --name="ConversionMonitorLauncher" launcher.py
echo ✅ Build complete! Check dist/ folder
pause
```

## 🎯 Переваги цього підходу:

✅ **Маленький EXE** (~10-15MB) - тільки лаунчер
✅ **Швидкі оновлення** - без перекомпіляції
✅ **Централізоване управління** - один файл на сервері
✅ **Автоматичні оновлення** - користувачі завжди мають свіжу версію
✅ **Кросплатформність** - працює на Windows/macOS/Linux
✅ **Офлайн режим** - кешує останню версію

## ⚠️ Важливо:

1. **Python має бути встановлений** на комп'ютері користувача
2. **Або включіть Python в EXE** (збільшить розмір до ~50MB):
   ```powershell
   pyinstaller --onefile --windowed --hidden-import=tkinter launcher.py
   ```

## 🚀 Альтернатива: Самодостатній EXE

Якщо потрібен повністю автономний EXE без Python:

```powershell
# Компілюємо основну програму
pyinstaller --onefile --windowed conversion_monitor.py

# Тепер маємо один EXE ~50MB з усім включеним
```

## 📊 Порівняння підходів:

| Підхід | Розмір EXE | Оновлення | Python потрібен |
|--------|-----------|-----------|-----------------|
| Launcher | ~15MB | Миттєві | Так |
| Повний EXE | ~50MB | Перекомпіляція | Ні |
| Hybrid | ~15MB + завантажений код | Миттєві | Ні (включений) |
